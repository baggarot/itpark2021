version: "3.8"

services:
  postgres:
    image: postgres:14.1
    restart: always
    environment:
      POSTGRES_DB: "pgbd"
      POSTGRES_USER: "pguser"
      POSTGRES_PASSWORD: "123"
    ports:
      - "5432:5432"
    expose:
      - "5432"